// import express from "express";
// import { listarAgencias, buscarAgencia, criarAgencia, atualizarAgencia, deletarAgencia } from "../controllers/agencia.controller";
// export const agenciaRouter = express.Router();

// agenciaRouter.get("/", listarAgencias);
// agenciaRouter.get("/:id", buscarAgencia);
// agenciaRouter.post("/", criarAgencia);
// agenciaRouter.put("/:id", atualizarAgencia);
// agenciaRouter.delete("/:id", deletarAgencia);